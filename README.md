A TypeScript + React task management app that lets users create, view, edit, and delete tasks through a clean dashboard and detail pages. It uses React Context with fully typed state management, TypeScript interfaces for data safety, and Auth0 for secure login and user authentication. The project focuses on building a scalable, type-safe frontend with proper form validation, error handling, and modern React patterns.
